"""
📊 3. src/sorting.py (Algoritmos) -> Para a outra pessoa
O que fica nele: Exclusivamente a lógica de ordenação (Quick Sort). Ele não deve imprimir nada na tela, apenas receber dados, ordenar e devolver.
O que você deve pedir para a pessoa fazer:

Ela deve importar o carregar_familias do structures.py.

Ela deve criar uma função chamada gerar_ranking_prioridade() [não obrigatoriamente, pode ser outro nome].

Essa função deve filtrar quem tem insegurança "Grave", rodar o Quick Sort para ordenar pela menor renda_sm e retornar a lista ordenada.

Esqueleto para passar para o colega:

Python
from src.structures import carregar_familias

def quick_sort(lista):
    # Colega implementa o quick sort aqui
    pass

def gerar_ranking_prioridade():
    # Colega busca familias, filtra as Graves e chama o quick_sort
    # Retorna lista ordenada
    return lista_ordenada


"""